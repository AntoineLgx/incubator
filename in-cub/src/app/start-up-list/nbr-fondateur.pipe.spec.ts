import { NbrFondateurPipe } from './nbr-fondateur.pipe';

describe('NbrFondateurPipe', () => {
  it('create an instance', () => {
    const pipe = new NbrFondateurPipe();
    expect(pipe).toBeTruthy();
  });
});
